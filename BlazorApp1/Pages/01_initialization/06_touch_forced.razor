@page "/01_initialization/06_touch_forced"

<Gantt @ref=gantt firstRender=init class="left-container" />

<style>
    .left-container {
    overflow: hidden;
    position: relative;
    width: 100%;
    height: 89vh;
    }
</style>

@code {
    private Gantt gantt;
    private object config = new { touch = "force" };
    private void init()
    {
        gantt.set_Config(config);
        gantt.init();
        gantt.parse(common.testdata.demo_tasks);
    }
}
